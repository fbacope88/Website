{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/creative-web-studio run build && pnpm --filter @workspace/api-server run build",
  "builds": [
    {
      "src": "artifacts/api-server/dist/index.mjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "artifacts/api-server/dist/index.mjs" }
  ]
}
